const courseModel=require('../models/course.model');
const feeModel=require('../models/fee.model');
const levelModel=require('../models/levels.model');
const nationality_typeModel=require('../models/nationality_type.model');


exports.retrieving_based_on_filter=(req,res) => {

    try{

        var FEE_TYPES=req.query.FEE_TYPES;
        

    }
    catch(err) {

    }
}